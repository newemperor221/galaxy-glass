#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/home/woioeow/galaxy-glass/astro/node_modules/.pnpm/astro@5.18.1_rollup@4.60.4_typescript@5.9.3/node_modules/esbuild/bin/esbuild
